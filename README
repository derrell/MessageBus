```
# We'll put the .class files in bin
mkdir -p bin
javac -d bin org/lipman/MessageBus/*.java

# Run the example code at the bottom of MessageBus.java
(cd bin && java org/lipman/MessageBus/MessageBus)
```
